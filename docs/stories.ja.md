# ユーザーストーリー (正本)

> この日本語版が正本。フェーズの区切りで英語版 [stories.md](./stories.md) に同期する。

形式: 1 ストーリー 1 行。自明でない場合のみ受け入れ条件を添える。フェーズごとにグループ化し、未割り当てのアイデアは Icebox へ。

> 状態: Phase 1 は 2026-07-07 のキックオフで精査済み。Phase 2 以降はロードマップから起こした初期ドラフトのままなので、pleroma-fe への実際の不満を観察して書き直していくこと。

## Phase 0 — 基盤

ユーザーストーリーの代わりに、一本の縦串を Done 条件とする:

- [x] dev proxy + 型生成済みクライアント経由でリファレンスインスタンスのタイムラインを取得し、アプリシェル (ラフ) 内に Status カードのリストとして描画できる。dev トークンは env から注入し、未設定なら 401/403 (エンドポイントで異なる — PLAN の落とし穴参照) がハンドリングされて UI に現れる。(2026-07-06 確認: 200 経路はブラウザ実機、401/403 はページテスト + 実インスタンスへの素の fetch で確認)

## Phase 1 — 閲覧 MVP

**Done 条件**: 下記ストーリーがすべてチェックされ、スマホ実機と PC の両方で成立している (片手での操作範囲、横スクロールなし)。かつ、閲覧については pleroma-fe を開かずに 1 日過ごせる。例外は通知 (2026-09-13): 通知は既読管理と組でなければ dogfooding にならず、既読を付ける API が全部 write scope なので Phase 2 の持ち物。通知を見るために pleroma-fe を開くのは Done の判定に数えない。

- [ ] インスタンスのアカウントでログインでき、ブラウザを再起動してもログイン状態が保持される。ログアウトすると確実に未認証状態に戻る (Akkoma は Bearer リクエストにセッション Cookie も返す — PLAN の落とし穴参照)。
  - 未ログインで開いた URL がログイン後も維持される (2026-08-09 実装完了。スレッドの permalink が共有可能な URL になり、「リンクを踏む → 未ログイン → サインイン → 投稿を見失う」が現実の痛みになったため、スレッド表示と同セッションで実装)。開始時のアプリ内パスを sessionStorage に保存し、復帰時に**パースしてから**使う — 同一オリジンの別コードが書ける保存領域なので、戻り先は信用できない入力として扱い、アプリ内パス以外は `/` に落とす。ブラウザでの往復は未検証 (未ログイン状態と authorize 同意が要るため)
- [ ] Home タイムラインを滑らかな無限スクロールで読める。CW を開いてもスクロール位置がずれない。
- [ ] 開きっぱなしのタイムラインで、ページを再読み込みせずに新着を読み込める。
  - 手動更新 (ボタンないしプルリフレッシュ)。前方取得 (`since_id`; 応答が満杯なら未取得の区間があるとみなしてセグメント境界の GapMarker から埋め直す) の合流処理は store / segment 層に持ち、UI からは切り離しておく — Phase 4 の Streaming / 自動更新トグルが同じ合流処理を再利用する
  - refresh ボタンで新着を読み込んだら最上端へスクロールする (2026-08-08 Icebox から昇格 — dogfooding で「トップに行かない」違和感が実際に出た。refresh は明示アクションなので新着を目の前に出す。読みかけ位置に戻りたいケースの設計 — 据え置きスクロールや gap row 動線の温存 — は実装時に判断)。2026-08-11 の dogfooding 初日にも再指摘 (2 回目) — 優先度上げの根拠。2026-08-15 に 3 回目の指摘、スレッド小修正の次・プロフィールページの前に着手すると決定。2026-08-16 実装完了: refresh 成功時は新着 0 件でも常にトップへ (明示アクションへの応答を状況で変えない)、失敗時は読み位置を維持。「読みかけ位置に戻る」動線はスコープ外と決定 — 満杯応答時の GapMarker から辿れる既存動線で足りる。ページ破棄後に届いた refresh が次のルートの読み位置を奪わないことも合わせて担保。ブラウザ実機確認待ち: happy-dom には layout がないため、深くスクロールした状態でフルページを prepend したときにブラウザのスクロールアンカリングが `scrollTo(0, 0)` と競合しないかはテストで到達できない
- [ ] home / local / bubble / federated を切り替えて読める。(2026-08-08 実装完了: パスベース 4 ルート + バー内タブ、切替時は再フェッチ (読み位置保持は Icebox 済み項目)。エンドポイント/クエリの対応はページテストで確認。PC ブラウザ実機で確認 (2026-08-08): 4 タブ切替と aria-current、`local=true` の実効 (ローカル投稿のみ)、bubble の中身、federated のリモート混在、無限スクロール + 手動更新、375px でタブ + refresh ボタンが本文横スクロールなしで収まる。スマホ実機確認待ち)
- [ ] Status カードで投稿の中身が一通り読める: 本文 (HTML)、添付メディア、CW/sensitive、カスタム絵文字、ブースト元、絵文字リアクション、投票、リンクプレビュー。(2026-07-13 実装完了。PC ブラウザ実機で確認: 引用ミニカード、リンクプレビュー、リモート絵文字リアクション、終了済み投票、sensitive + blurhash 欠落フォールバック、CW 開閉のスクロール非ずれ、375px 横スクロールなし。スマホ実機確認待ち。進行中投票はページテストのみ、video 添付は実データ未遭遇のため未検証)
  - 本文はサニタイズ済み HTML として描画。`<small>` など pleroma-fe が取りこぼす基本タグも正しく表示する。MFM 専用描画には対応しない (`content` の HTML をそのまま描画)
  - メディアは 2 列グリッドで全枚数表示 (1 枚なら全幅)、タップで簡易オーバーレイ原寸。動画/音声はネイティブ `<video>`/`<audio>`。スワイプ送り付きライトボックスは Icebox。リモート添付の `blurhash`/`meta` 欠落で落ちない。sensitive メディアはデフォルトで隠れる
  - 投票は閲覧のみ (選択肢・票数・締切/終了状態)。リンクプレビューはタイトル+サムネイルの軽量版
  - 本文中のメンションはアプリ内のプロフィールへ。外部リンクは新しいタブで開く (ADR-0011)
  - 引用は深さ 1 のミニカードとして本文下に描画 (2026-07-13 決定 — ADR-0007)。描画時は本文の `span.quote-inline` (サーバ付加の RE: リンク) を除去。`quote` が null (未取得リモート等) やミニカード内のさらなる引用は RE: リンクのまま残す
  - 絵文字リアクション・ブースト・ファボの「誰が」一覧はカードには置かず、投稿詳細 (スレッド) ビューの持ち物 (会話ツリーのストーリーで実装)
- [ ] 投稿を開いて会話ツリー全体を読める。どの投稿から来たのか見失わない。(2026-08-09 実装完了 — ADR-0014。カード全体タップで `/statuses/:id` へ、枝優先・インデント 0・折りたたみなし、`/context` の ancestors/descendants は信用せず `in_reply_to_id` からツリーを再構築。ブラウザ実機で確認: 起点への着地、スレッドからスレッドへ潜っての再着地と戻ったときの復元、メンション/外部リンク/メディア/テキスト選択がカードのタップに吸われないこと、375px 横スクロールなし。引用ミニカードと `<video>`/`<audio>` は実データ未遭遇のためページテストのみ。スマホ実機確認待ち)
  - 親がインスタンス未取得 (`in_reply_to_id: "_"` — PLAN の落とし穴参照) の場合、**未取得の時点でプレースホルダーを出し、そこに明示的な取り込み操作を置く**。自動 resolve はしない — 1 回 2.6 秒かかるうえインスタンス DB への書き込みとリモートへのリクエストであり、さらに `/api/v2/search` は解決できないと AP ID の全文検索にフォールバックするので黙って別の投稿を親として表示しうる (ADR-0011 amendment)。元インスタンスへのリンクは外部と明示した副次的な導線としてボタンの隣に添える
  - **リモート投稿を明示的に取り込めること自体が差別化点** — akkoma-fe にこの機能は無く、`"_"` が truthy なので死んだリンクを出す (2026-08-09 にコード追跡で確認)
  - スレッドを開いて戻ったときにタイムラインの読み位置と蓄積したページを失わない (2026-08-09 実装完了、スレッド本体より先行。ブラウザ実機で確認: 185 件蓄積して離脱・復帰で全件と `scrollY` が復元され再取得なし、タブ切替は従来どおり再取得。ADR-0004 amendment)。**戻る導線は history back でなければならない** — スクロール復元は pop でのみ効く
- [ ] 絵文字リアクション・ブースト・ファボの「誰が」一覧をスレッド (投稿詳細) から開ける (`/api/v1/pleroma/statuses/:id/reactions`、`/reblogged_by`、`/favourited_by` — 形は実装セッション冒頭に実測)。2026-08-09 に会話ツリーのストーリーから分離: 一覧の設計 (どこに出すか、誰の一覧を優先するか) は会話ツリーとは独立した判断で、まとめると片方の未確定がもう片方を止める
  - 2026-09-13 実装完了 (branch `feat/who-lists`): 1 ページ + タブ 3 本 `/statuses/:id/favourited_by` / `/reblogged_by` / `/reactions`。タブはパスのセグメント (`?type=` ではない — `<A>` の `aria-current` はパス一致でしか付かず、既存のタブバー 2 本と同じ慣習)。入口は**スレッドの起点カードのみ**: ファボ / ブーストの数字と各リアクションチップが該当タブへのリンクになる。タイムラインカードは触らない — Phase 2 で数字が操作 (ファボ / ブースト) になる見通しで、X と Mastodon web も同じ分け方 (akkoma-fe / Elk / Misskey はタイムラインでも出すが hover 経由で、タップ主体では成立しない)。3 API ともページネーション無し (PLAN の落とし穴) なので router `query` + `createAsync` 1 本ずつ、`cursor-list-store` も retention snapshot も使わない (`markRetentionFrame` だけ積む)。reactions は 1 ページに絵文字ごとのグループ見出し (カードと同じチップ) + アカウント行で、per-emoji ルートは持たない。404 / 403 は Retry 付きのエラー行、`[]` (非公開・`show_reactions: false` 込み) は空文言で、区別しない。空文言は "No favourites to show." のようにゼロを断定しない — タブには投稿の count がそのまま出るので、Akkoma が一覧を伏せる (`show_reactions: false`) か知らないアカウント・ブロック済みを落とすと count と行数が食い違う。タブ切替は `replace` (同じページ内の移動なので履歴を増やさず、Back 1 回でスレッドへ。プロフィールのタブは push のまま)。見出し・タブの count・一覧はページ内の `<Suspense>` で独立に届く — レイアウトの境界に落とすと直接到着で面ごと隠れ、`threadQuery` の `/context` まで待つことになる。ブラウザ確認済み (2026-09-13、PC): 起点カードの数字 → 一覧、タブ切替、空文言、カスタム絵文字のグループ、Back でスレッドに再取得なしで戻る、375px で 3 タブが横スクロールなし。スマホ実機確認待ち
  - **入口を統計行に変更** (2026-09-14 PC dogfooding 指摘、branch `feat/thread-stats-row`): ファボ / ブーストのアイコン横の数字とリアクションチップをリンクにする案は、アイコンのアフォーダンスが「実行する」であり「誰がしたか見る」ではないため撤回 (Phase 2 でアイコンが本当に操作になると同居できない)。起点カードのみ、チップと ActionBar の間に「N boosts · N favourites · N reactions」の統計行を置き、各項目が該当タブへのリンク。起点カードの ActionBar は数字を消してアイコンだけにする (同じ数字を 2 度出さない)。0 件の項目は行から消し、全部 0 なら行ごと出さない — 一覧が伏せられて count と行数が食い違う場面を減らす。チップはリンクを持たない (Phase 2 でタップ = リアクション)。タイムライン・祖先・子孫のカードはアイコン + 数字のまま、リンク無し。Mastodon web (投稿詳細の meta 行、アクションバーは `withCounters` 無し) / Bluesky (スレッド起点の "N reposts · N likes"、0 件は消える) / akkoma-fe (focused のみ `favs-repeated-users` 行) と同型 (2026-09-14 にソースで確認)。ブラウザ確認済み (PC、375px 含む)
- [ ] プロフィールを投稿 / 返信 / メディアのタブ付きで閲覧できる。フォロー関係 (フォロー中 / されている) が見える。
  - フォロー一覧 (フォロー中 / フォロワーの一覧) のタブもこのストーリーに含む (2026-08-30 明記)
  - 着手順 (2026-08-30 決定): #1 ヘッダ + 投稿リスト (`exclude_replies=true`。Akkoma はこのフラグでセルフリプライも落とすが、クライアント側フィルタはページネーションを壊すため癖ごと受容 — 実測で確認済み) → #2 タブ UI + 投稿と返信 / メディア + 関係バッジ → #3 フォロー一覧タブ → pinned 表示は最後。フォロー操作は Phase 2。ワイヤーは [docs/design/profile-page-20260830.html](./design/profile-page-20260830.html)
  - 投稿詳細を開いて戻ったときにプロフィールの読み位置と蓄積したページを失わない (2026-09-11 dogfooding 指摘。プロフィールルートは TimelineRetention の内側にあるが slot が `TimelineSnapshot` 1 スロット固定で葉が claim しないため、戻ると先頭から再取得になり、タイムライン → スレッド → 戻るの挙動と食い違う。slot の snapshot を path キーの汎用型に広げて乗せる — **ADR-0004 amendment 候補**。プロフィール動線まとめの後、#3 の前に着手: #3 のフォロー一覧が 3 件目のリストになるので、その前に形を決める)。2026-09-12 実装完了 (branch `feat/profile-round-trip`、動線まとめと同じ PR): retention を「path キーの履歴 stack、復元は pop のときだけ」に置き換え `src/entities/retention/` へ移動 — ADR-0004 amendment (2026-09-12)。プロフィールは `{statuses, exhausted}` を snapshot に持ち、スレッドは空 frame を積む。ブラウザ確認は dogfooding で
  - #3 フォロー一覧 (2026-09-13 実装完了、branch `feat/profile-follow-lists`): `/users/:acct/following` / `/followers` を葉ルート 2 本として追加、タブバーは投稿 3 タブのまま (5 タブは 375px で折れる)。入口はヘッダの count で、2 軸を独立に扱う — `hide_*_count` なら数字の代わりに "hidden"、`hide_*` (一覧そのもの) が立っていればリンクを付けない。非公開一覧は Akkoma が本人以外に 200 + `[]` を返すので「フラグ + 空応答」で文言を出し、本人判定はしない (本人にも "hidden" と出る上、`hide_*` を立てた本人は自分の一覧への入口リンクも失う — URL 直打ちなら Akkoma は本人に実一覧を返す — のは既知の割り切り。verify_credentials の id を持つのは Phase 2 のフォロー操作と同じスロット)。行はアバター + 表示名 + acct の 1 anchor、bio / 関係バッジは持たない。一覧の `:id` は flake id のみで nickname は 404 (PLAN の落とし穴)。投稿リストと同じカーソル蓄積ストアを共用 (`src/pages/profile/cursor-list-store.ts`)。ブラウザ確認は dogfooding で (プロフィール往復の歩き方に「count → 一覧 → 行 → 別プロフィール → Back → Back」を足す)
  - pinned 表示 (2026-09-13 実装完了、branch `feat/profile-pinned`): Posts タブの先頭に `?pinned=true` で取った投稿を "Pinned" マーカー付きで並べる。設計は akkoma-fe / Mastodon web / Phanpy / Elk の 4 クライアントに揃えた — Posts タブのみ、別 query (`pinnedQuery`) で retention snapshot の対象外、時系列リストとの dedupe はしない (同じ投稿が 2 回出てよく、マーカーは帯側だけ。唯一 dedupe を試みる Phanpy は自ら hack と注記している)、取得にタブの filter は付けない (Akkoma は `pinned` と `exclude_replies` を AND で結ぶ — PLAN の落とし穴)。リモートアカウントは複数件入るので帯は複数行になりうる。既知の限界: ブラウザ Back では solid-router の query キャッシュが無条件に当たるので strip は fetch なしで戻るが、3 分超のエントリは GC されうるため、長くスレッドに滞在してから戻ると strip が復元後に挿入されて読み位置がその高さ分ずれる。dogfooding で気になったら snapshot に載せる。ブラウザ確認は dogfooding で (自分のプロフィールで 1 件、misskey.io のアカウントで 3 件、Replies / Media で出ない)
  - 2026-09-14 PC dogfooding の修正 (branch `fix/profile-dogfooding`): 表示名がヘッダ画像に被る (名前ブロックがアバターと同じ行ごと引き上げられていた — アバターだけが帯に掛かり、名前は帯の下端から始める) / Following・Follows you の枠を外し `bg.subtle` の pill に (枠付きはリンクの隣で押せそうに見える) / following・followers 一覧の上では投稿 3 タブを出さない (一覧が自分の見出しを持つ。判定は `useMatch`、location 文字列は読まない) / "N posts" も Posts タブへのリンクに (3 つの count が同じ振る舞い) / **ヘッダの count リンクと 3 タブは `noScroll`**: ヘッダが残ったまま下だけ差し替わるページ内のセクション切替であって到着ではないので、push のスクロール先頭化を止める (誰が一覧のタブを `replace` にしたのと同じ理屈。Back の復元は pop 側なので影響なし)。加えて差し替え中の "Loading…" 行に `minH: 100dvh` — 一瞬 1 行に縮むと文書が viewport より短くなり、ブラウザが scrollY を 0 に丸めてしまう (実測: `scrollTo` は呼ばれていないのに 400 → 0)。届いた一覧が viewport より短ければ最終的に 0 になるのは正しい挙動
  - ヘッダのワードマーク "utaita" をインスタンス名にするかは保留 (2026-09-11)。`/api/v1/instance` の query がまだ無く、Phase 2 で投稿の文字数上限・添付上限を取りに行くときに同じ query で名前も取れるので、そこで判断
- [x] プロフィール動線まとめ (2026-09-11 dogfooding 指摘。プロフィール #2 のマージ後、フォロー一覧 #3 の前に着手 — これが無いとプロフィールは本文中のメンション経由か URL 直打ちでしか着けない): StatusCard のアバターと名前ブロックを `/users/:acct` へのリンクにする (カード全体タップの会話オープンに吸われないこと — メンションと同じ扱い。ページテストで確認) / ヘッダのワードマークをホームタイムラインへのリンクにする (プロフィールに Back ボタンは置かない — スレッドの Back は history back でしかスクロール復元が効かない事情から来たもので、ワードマーク = ホーム先頭で足りる) / リモートアカウントのヘッダに元サーバーのプロフィール (`account.url`) への外部リンクを添える (ローカルには受信した投稿しか無いため。新しいタブで開く — ADR-0011) / 読み込み中に縦スクロールバーが消えてカラムが右にずれるのを `scrollbar-gutter: stable` で止める (globalCss に 1 行、タイムラインの読み込み中にも効く)。2026-09-12 実装完了 (branch `feat/profile-round-trip`): アバターと名前ブロックは `<A>` 2 本 (アバター側は `tabIndex=-1` + `aria-hidden` でタブ停止を二重にしない)、ワードマークは `/` へのリンク、リモート acct のヘッダに `View on <host>` の外部リンク、`scrollbar-gutter: stable` は globalCss。ブラウザ確認は dogfooding で
- [x] タイムラインとスレッドの status 表示を full-bleed + 罫線に改める (2026-08-11 dogfooding 指摘 4・5・7 の統合。per-status のカード枠・角丸・gap が縦密度と本文幅を削るため廃止し、行 (左右 inset 12px + border-bottom 1px) にする。デスクトップはカラム単位で panel を着せ、QuoteCard は引用の異質さを示す信号としてカードのまま。スレッドは背骨ルールを画面端に置き本文 inset を左右対称にする — subject の 2 チャンネルマークは維持。判断理由と棄却案は [docs/design/timeline-density.md](./design/timeline-density.md))
- [x] スレッド画面の小修正まとめ (2026-08-11 dogfooding 指摘 3・8・9・11・14。密度改修の後に着手 — 見た目の土台が先): Back ボタンのアイコン化と "Conversation" 見出しの整理 (canGoBack の出し分けは維持、SR 向け見出しの残し方は実装時判断) / スレッド内の投稿時刻を時刻精度で表示 (タイムラインカードは日付のまま。time.ts のロケール "en" には触れない — Phase 4) / UnfetchedParent をプレースホルダーカード化 (取得成功時のフォーカス移動先の設計と同時に — 起票済みチップ参照) / カスタム絵文字の拡大 (本文 1.25em ≈ 20px は akkoma-fe 実測 38px の約半分。新密度の画面で視覚調整) / "Not connected to this post" セクションの見出し + 説明文を面の先頭のヘッダー帯に収める (2026-08-11 指摘 14 追加 — 密度改修で前後が面になり、canvas 上の裸のテキストの浮きが目立つようになった) / 絵文字リアクションのチップも本文と同寸に拡大 (2026-08-15 指摘 15 追加 — 本文を 2em にしたことでチップの実寸 15px が相対的に目立つようになった。akkoma-fe も本文とリアクションを同寸で描く。件数の数字はメタ情報として xs 据え置き)

## Phase 2 — 書き込み

- [ ] 通知を読め、どこまで読んだかが分かる。メンション・ブースト・フォロー・絵文字リアクションが一目で区別でき、`pleroma:emoji_reaction` や `move` など未知の type で落ちない。未読が見分けられ、閲覧で既読になる。
  - 2026-09-13 に Phase 1 から移動。既読を付ける経路 (`POST /api/v1/pleroma/notifications/read` は `write:notifications`、`POST /api/v1/markers` は `follow` / `write:blocks`) がすべて write scope で、Phase 1 の `read` トークンでは `pleroma.is_seen` が全件 false に張り付いて未読表示が成立しない。一覧だけ先に Phase 1 で出す案は、既読前提で行と store を 2 回設計することになるため棄却。着手はトークン scope の変更 (`read` → `read write`、ログイン画面の story 参照) の直後
- [ ] CW・公開範囲 (`local` 含む) 付きで投稿でき、カスタム絵文字の補完が効く。
- [ ] alt text 付きで画像を添付でき、アップロードの進捗が見える。
- [ ] タイムライン上からファボ・ブースト・ブックマーク・絵文字リアクションができる。
- [ ] プロフィールからフォロー / 解除でき、承認制アカウントには申請中の状態が表示される。
- [ ] アカウント・ハッシュタグ・投稿を検索できる。
- [ ] お気に入り / ブックマークした投稿の一覧を閲覧できる (`/api/v1/favourites`, `/api/v1/bookmarks`)。
- [ ] ログイン画面をワイヤー ([docs/design/login-20260712.html](./design/login-20260712.html)) に合わせて仕上げる (2026-08-11 dogfooding 指摘。Phase 1 では意図的に直さない — 接触頻度が低く、Phase 2 のトークン scope 変更 (`read` → `read write`) で全員が再認可のためログインを通り直すタイミングに実装するのが同スロットで安い)

## Icebox

- 見逃した投稿のキャッチアップダイジェスト (Phanpy 風)
- タブを閉じても消えない下書き
- Streaming によるタイムラインのリアルタイム更新 — 自動更新 / 手動更新をトグルで切り替えられる形で (昔の Twitter クライアント風)
- 複数アカウント
- タイムラインごとのスクロール位置保持 (Phase 1 キックオフで削除 — home 常駐の利用実態では益が薄い。dogfooding で必要になったら)。2026-08-09 に**一部だけ昇格**: 「直前に見ていた 1 本を、詳細ルート (スレッド / プロフィール) へ出て戻る間だけ保持する」は実装済み (ADR-0004 amendment)。スレッド表示が入ると「返信を開いて戻る」が最頻の操作になり、そのたびに先頭から再取得するのは Done 条件に届かないため。**タイムライン間の切替での保持はここに残す** — 切替は push でスクロール復元が効かず、保持すると「古い内容・先頭・フェッチなし」という再取得より悪い状態になる
- スワイプ送り・ピンチズーム付きのメディアライトボックス
- 詳細ルートの Back 制御が再入場で誤る (2026-09-13 レビュー指摘): スレッドと投稿下の一覧は preload の `intent !== "initial"` で `canGoBack` を決めるが、直接到着 (またはサインイン復帰) → プロフィールへ push → ブラウザ Back で再入場すると preload が `intent: "native"` で再実行され、投稿へのリンクだったはずの Back が history back ボタンになってアプリ外 (認可ページ) へ戻れてしまう。`src/pages/thread/thread-query.ts` と `who-lists-query.ts` の共通ルールなので、直すなら「そのエントリの前にアプリ内エントリがあるか」を history state か retention の frame に持たせて両方同時に
- ルート到着時のフォーカス移動 (2026-09-13 レビュー指摘): キーボードでスレッドの count → 一覧、カード → プロフィールのように遷移すると、押したリンクがルート切替で消えてフォーカスが `body` に落ち、スクリーンリーダーに到着が伝わらない。個別ページに足す話ではなく、明示遷移のときだけ見出しや Back に移し、履歴 traversal では奪わない、というアプリ横断の設計が先
- ハッシュタグタイムライン (`/api/v1/timelines/tag/{tag}`)
- リストタイムライン (`/api/v1/timelines/list/{list_id}`)
- DM の閲覧 (spec のタイムライン一覧に direct が無い — Akkoma の chats 機構の調査から)
- リモートにしか存在しない返信の補完 (発言元インスタンスの公開 API をマージする差別化機能。同一オリジン前提と衝突するため要設計)
- カード時刻表示の再設計: 相対時刻 ("5m") が良いデザインかは要議論 — dogfooding で不満が出るか観察し、材料が揃い次第議論。2026-08-11 dogfooding: タイムラインカード側には不満なし。スレッド画面の時刻表示だけを Phase 1 の小修正として分離・先行し、全体再設計 (精度・TZ 設定) はここで観察継続。現状の絶対時刻はカレンダー表示 ("Jul 5") がブラウザのローカル TZ (Intl デフォルト)、ツールチップ (`title`) は API の生 ISO 文字列。理想は利用者が設定でタイムゾーンを選べること。実装スロットは Phase 4 の i18n と同時 (time.ts はロケールも "en" ハードコードで、同じ箇所を触るため — PLAN 参照)
- About 画面 + `/THIRD_PARTY_LICENSES.md` へのリンク(Vite の `build.license` で自動生成)。同時にバグ報告リンク等を `package.json` の `bugs.url` から import する形にする。attribution ファイルの補完性(現状 blurhash のように upstream で LICENSE 欠落の依存が空 body で並ぶ)は実装時に判断し、必要なら `rollup-plugin-license` で SPDX テキストを合成する fallback を検討。実装スロットは i18n / 設定画面ができるタイミング — Phase 4 想定
- closed な poll の最終結果を re-fetch する (2026-08-11 dogfooding 指摘。リモート発の poll は連合で届いた時点の票数しか無い。Akkoma ソース実査済み: `GET /api/v1/polls/:id` が唯一の refetch 誘発 API — リモートオブジェクトかつ前回更新から 60 秒超なら発言元へ HTTP GET して票数・closed を取り込む。openapi.json に定義あり。実装案: スレッド表示時に polls/:id を 1 発叩いて store の poll を差し替える。表示ごとの追加リクエスト 1 本の価値と、リモート先が落ちている場合の遅延・失敗の扱いが論点)
- 翻訳ボタン (2026-08-11 dogfooding 指摘。akkoma-fe 同等機能。API は openapi.json に一式: Akkoma 式 `GET /statuses/{id}/translations/{language}` と Mastodon 式 `POST /statuses/{id}/translate`、対応言語一覧 `/api/v1/instance/translation_languages`。エンドポイント選択と UI は実装時設計。前提: インスタンス側に翻訳バックエンド設定 — ringed.space は DeepL 設定済みで検証可能。派生タスク: README 拡充 — インスタンス設定に依存する機能とその前提を運用者向けに英語で明記し、未設定インスタンスでの UI の振る舞いも決めて揃える)
- 長文投稿の「もっと見る」折りたたみ (2026-08-11 dogfooding 指摘。akkoma-fe は max-height 超過で "Show more"。CW の開閉機構に「高さ超過による折りたたみ」を乗せられるかは実装時検討。CW で実績のある「開閉してもスクロール位置がずれない」を折りたたみ解除でも守ること)
- タイムラインの older 取得を per-anchor に並列化 — 現在は複数 GapMarker/Sentinel の要求を単一キューで直列消化しており、他アンカーの完了を待つ体感がある。並列化すると `appendOlder` の merge 経路 (先行応答で anchor 対象セグメントが吸収される可能性) と `exhausted` の帰属条件を再設計する必要があるため、dogfooding で「他 gap の待ちが実害になる頻度」を観察してから判断。
